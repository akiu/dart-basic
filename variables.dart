/*
* 
*	Variables in Dart 
* 
* 
*/

var answer = 42; //dynamic variables
var result; // what is this ??
int result; //type annotation, it will throw an error, if this variable is assigned by string (checked mode)
var age; // == null, if you dont assign value to it

final name = "bob";
final String name = "bob";

name = "Alice" // use final, to tell that variable cannot be reassigned